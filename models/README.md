# Phishing Demo Bundle
Created: 20251022_103620

**Guarantees**
- RNN ckpt present (original or synthesized) + `rnn_url_vocab.json` → URL inference won’t be NaN.
- Thresholds ensured: `rnn_best_threshold.json`, `transformer_best_threshold.json`, `gnn_best_threshold.json`, `gnn_best_threshold_calibrated.json`, `ensemble_val_best_threshold.json`.
